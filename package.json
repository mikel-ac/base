{
  "name": "base-core",
  "version": "0.1.0",
  "description": "Base · esqueleto y lógica (dominio, datos, estado). Sin UI.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "sanity": "tsc && node dist/scripts/sanity.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
